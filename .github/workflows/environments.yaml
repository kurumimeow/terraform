name: Main Environment Pipeline

on:
  push:
    branches:
      - main
      - feature/*
  workflow_dispatch:

jobs:
  # DEV
  dev-plan:
    uses: ./.github/workflows/env-template.yaml
    with:
      environment: dev
      step: plan

  dev-apply:
    needs: dev-plan
    uses: ./.github/workflows/env-template.yaml
    with:
      environment: dev
      step: apply

  # NPR
  npr-plan:
    needs: dev-apply
    uses: ./.github/workflows/env-template.yaml
    with:
      environment: npr
      step: plan

  npr-apply:
    needs: npr-plan
    uses: ./.github/workflows/env-template.yaml
    with:
      environment: npr
      step: apply

  # PRD
  prd-plan:
    needs: npr-apply
    uses: ./.github/workflows/env-template.yaml
    with:
      environment: prd
      step: plan

  prd-apply:
    needs: prd-plan
    uses: ./.github/workflows/env-template.yaml
    with:
      environment: prd
      step: apply
