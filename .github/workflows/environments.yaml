name: Main Environment Pipeline

on:
  push:
    branches:
      - main
      - feature/*
  workflow_dispatch:

jobs:
  deploy-dev:
    uses: ./.github/workflows/templates/env-template.yaml
    with:
      environment: dev

  deploy-npr:
    uses: ./.github/workflows/templates/env-template.yaml
    with:
      environment: npr

  deploy-prd:
    uses: ./.github/workflows/templates/env-template.yaml
    with:
      environment: prd
