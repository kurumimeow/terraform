name: Main Environment Pipeline

on:
  push:
    branches:
      - feature/*
  workflow_dispatch:
    inputs:
      skip_dev:
        description: 'Skip DEV Environment'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'
      skip_npr:
        description: 'Skip NPR Environment'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'
      skip_prd:
        description: 'Skip PRD Environment'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'

jobs:
  # DEV
  dev-plan:
    name: DEV
    needs: ${{ github.event.inputs.skip_dev != 'true' }}
    if: ${{ github.event_name != 'push' || startsWith(github.ref, 'refs/heads/feature/') }}
    uses: ./.github/workflows/env-template.yaml
    with:
      environment: dev
      step: plan

  dev-apply:
    name: DEV
    needs: dev-plan
    if: ${{ github.event_name != 'push' || startsWith(github.ref, 'refs/heads/feature/') }}
    uses: ./.github/workflows/env-template.yaml
    with:
      environment: dev
      step: apply

  # NPR
  npr-plan:
    name: NPR
    needs: ${{ github.event.inputs.skip_npr != 'true' }}
    if: ${{ github.event_name != 'push' || startsWith(github.ref, 'refs/heads/feature/') }}
    uses: ./.github/workflows/env-template.yaml
    with:
      environment: npr
      step: apply

  npr-apply:
    name: NPR
    needs: npr-plan
    if: ${{ github.event_name != 'push' || startsWith(github.ref, 'refs/heads/feature/') }}
    uses: ./.github/workflows/env-template.yaml
    with:
      environment: npr
      step: apply

  # PRD
  prd-plan:
    name: PRD
    needs: ${{ github.event.inputs.skip_prd != 'true' }}
    if: ${{ github.event_name != 'push' || startsWith(github.ref, 'refs/heads/feature/') }}
    uses: ./.github/workflows/env-template.yaml
    with:
      environment: prd
      step: plan

  prd-apply:
    name: PRD
    needs: prd-plan
    if: ${{ github.event_name != 'push' || startsWith(github.ref, 'refs/heads/feature/') }}
    uses: ./.github/workflows/env-template.yaml
    with:
      environment: prd
      step: apply
